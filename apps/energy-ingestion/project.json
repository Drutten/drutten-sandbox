{
  "name": "energy-ingestion",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/energy-ingestion/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/energy-ingestion",
        "format": ["cjs"],
        "bundle": true,
        "main": "apps/energy-ingestion/src/main.ts",
        "tsConfig": "apps/energy-ingestion/tsconfig.app.json",
        "assets": ["apps/energy-ingestion/src/assets"]
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "energy-ingestion:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "energy-ingestion:build:development"
        },
        "production": {
          "buildTarget": "energy-ingestion:build:production"
        }
      }
    },
    "docker-build": {
      "dependsOn": ["build"],
      "command": "docker build -f apps/energy-ingestion/Dockerfile . -t energy-ingestion"
    }
  }
}
